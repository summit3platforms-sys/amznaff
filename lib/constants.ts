// Sitewide "prices last verified" date. Update this whenever prices in
// data/products/*.ts are manually re-checked against Amazon. Once a real
// price-refresh pipeline exists (see PA-API discussion), this can become
// per-product instead of sitewide.
export const PRICES_LAST_VERIFIED = 'July 8, 2026';
