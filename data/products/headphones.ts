import { Product } from '../types';

// ---------------------------------------------------------------------------
// Headphones category — product data
//
// Intentionally empty. The initial seed/demo products used to scaffold the
// site have been removed so real research and real product data can replace
// them. Every product page, comparison page, and filter page in the app is
// generated from whatever objects live in this array — see
// lib/content-generator.ts and lib/scoring.ts for how copy and rankings are
// derived (never invented) from structured data. Add real products here
// (with verified specs, real Amazon ASINs, and real photography) when this
// category is ready to go live again.
// ---------------------------------------------------------------------------

export const headphonesProducts: Product[] = [];
